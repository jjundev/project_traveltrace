# PRD: 여행 사진 경로 리플레이 앱 (가칭: TravelTrace)

> 갤러리의 여행 사진을 골라 랜드마크·위치를 인식하고, 지도 위에 촬영 시각순 이동 경로를 그려
> "구글어스 위를 날아다니며" 여행을 다시 떠나는 슬라이드쇼로 보여주는 Android 앱.

> **개정 메모**
> - v2: grill-review 1차 — 5개 Blocker(폐기된 GPT-4o, AI 좌표 환각, 표준 Maps SDK의 3D 한계,
>   Photo Picker의 GPS, 휘발성 URI 캐시 키) 반영.
> - v3: grill-review 2차 — 3D를 best-effort로 격하 + 강등 계약 명시(#3), Photo Picker 배제 근거 정정(#1),
>   Places vs Geocoding 분기(#4), 동시성·재시도 수치 확정(#8), 네이티브 스키마 출력(#9),
>   전송 파이프라인 순서 명시(#10), 도난 키·다중 타임존 잔여 위험 보강(#5/#6).
> - v4: grill-review 3차 — 비-JPEG 원본도 업로드 전 항상 재인코딩(EXIF 스트립 보장), 무타임존 폴백을
>   기기 타임존+경고로 확정, Photo Picker 배제 근거를 영구성→안정적 `_ID`로 정정, "도난 키 방어" 라벨을
>   "피해 범위 제한"으로 수정, "오프라인 재생" 표현 정정(지도 타일은 네트워크 필요).
> - v5: 개발 언어를 **Kotlin/Jetpack Compose → Java + View/XML**로 변경. UI는 Fragment + Navigation
>   Component(XML nav graph) + ViewBinding, 비동기는 코루틴 대신 ExecutorService(백그라운드 스레드),
>   JSON 직렬화는 kotlinx.serialization 대신 Gson, Room/Hilt는 KSP 대신 annotationProcessor.
>   Gradle은 Kotlin DSL → Groovy DSL, buildSrc도 Java. 인식 엔진·좌표화·우선순위 등 제품 로직은 불변.

---

## 1. 개요 (Overview)

| 항목 | 내용 |
|------|------|
| 제품명 | TravelTrace (가칭) |
| 플랫폼 | Android 전용 |
| 배포 범위 | 개인/가족용 (Play 스토어 미등록, APK 직접 설치) |
| 핵심 사용자 | 여행을 즐기는 일반인 — 자신의 여행 사진을 돌아보고 가족에게 보여주고 싶은 사람 |
| 한 줄 가치 | "여행 사진을 고르기만 하면, 그 여행을 지도 위에서 다시 떠난다." |

> **핵심 가치 정의:** 앱의 본질 가치는 **촬영 시각순 경로 리플레이**다. 3D "비행" 연출은 부가(best-effort)
> 기능이며, 3D가 불가하더라도 2D 경로 리플레이만으로 제품이 완결되어야 한다(4.4 강등 계약 참조).

---

## 2. 문제 정의 (Problem)

- 여행을 다녀오면 수백 장의 사진이 갤러리에 남지만, **시간이 지나면 "이게 어디였더라?"** 하고 기억이 흐려진다.
- 사진을 시간·장소 순으로 다시 정리해 **여행의 흐름을 한눈에 보는** 쉬운 방법이 없다.
- 가족·지인에게 여행을 보여줄 때 사진을 한 장씩 넘기는 것은 밋밋하다.

---

## 3. 핵심 사용자 시나리오 (User Story)

1. 아빠가 파리 여행에서 찍은 사진 80여 장을 갤러리에서 선택한다.
2. 앱이 각 사진의 GPS·촬영 시각을 읽고, GPS가 없는 사진은 AI로 랜드마크/위치를 추론한다.
3. 인식이 끝나면 지도 위에 에펠탑 → 루브르 → 센강 순으로 핀과 경로가 그려진다.
   (AI로 추정한 위치는 "근사 위치"로 시각 구분되어 표시된다.)
4. "재생"을 누르면 (3D 가용 시) 카메라가 3D로 기울어진 채 첫 장소로 날아 내려가고,
   도착할 때마다 그 위치의 사진을 카드로 보여준 뒤 다음 장소로 이동한다.
   (3D 불가 시 2D 경로 위에서 동일한 리플레이가 진행된다.)
5. 마음에 드는 사진에서 일시정지하거나 수동으로 앞뒤로 넘긴다.
6. 이 결과를 "2024 파리 여행"으로 저장해두고, 나중에 가족에게 다시 보여준다.

---

## 4. 기능 요구사항 (Functional Requirements)

### 4.1 사진 선택
- 기기 갤러리에서 **다중 선택** (한 번에 최대 ~100장 권장).
- **`MediaStore` 기반 선택 + `READ_MEDIA_IMAGES` 권한 사용.**
  - 채택 사유는 **콘텐츠 해시 캐시 키(4.7)에 쓸 안정적 `MediaStore _ID` 핸들 + 재선택 없는 재조회**다.
    Photo Picker의 영구 grant(`takePersistableUriPermission`, API 30+ 상한 512개)는 동작하지만
    안정적 `_ID`를 주지 못하고 재조회 시 사용자 재선택을 강제한다. 즉 *영구성 자체*는 차별점이 아니며,
    캐싱·재조회를 위한 **안정적 식별자**가 MediaStore를 택하는 진짜 이유다.
  - (참고) Photo Picker도 `ACCESS_MEDIA_LOCATION` + `setRequireOriginal()`로 GPS 추출 자체는 가능하다.
    배제 이유는 GPS가 아니다.
  - **트레이드오프 명시:** `READ_MEDIA_IMAGES`(광범위 미디어 권한)는 향후 **Play 스토어 정식 배포를 막는**
    선택이다. 현재 배포 범위(sideload 가족용)에서는 수용하며, 상용 전환 시 권한 전략을 재검토한다.
- 사진 접근 권한 요청 및 거부 시 안내 처리.

### 4.2 위치 결정 (우선순위 규칙)
사진별로 아래 순서로 위치를 결정한다:

1. **EXIF GPS 존재 시 → 그 좌표 사용** (가장 정확, AI 호출 없음).
   - 원본 GPS 추출에는 **`ACCESS_MEDIA_LOCATION` 권한 + `MediaStore.setRequireOriginal()`** 적용이 필수
     (scoped storage는 기본적으로 위치 EXIF를 가린다).
2. **EXIF GPS 없음 → AI에 사진 전송해 랜드마크/도시 *이름* 추론 → 지오코딩으로 좌표 변환.**
   - AI가 직접 반환하는 lat/lng는 신뢰하지 않는다(환각·도시 중심점 수준). 4.3 참조.
3. **GPS도 없고 AI도 인식 실패 → "위치 미상"으로 분류** (4.6 참조).

> **시각 정규화:** `DateTimeOriginal` + `OffsetTimeOriginal`(있으면)로 **UTC 정규화** 후 정렬한다.
> 오프셋 정보가 없으면 사용자에게 **여행 기준 타임존 1개**를 지정받는다.
> - **무타임존 폴백:** 오프셋 태그도 없고 사용자가 타임존도 지정하지 않으면 **기기 현재 타임존을 기본 적용**하고
>   "정확한 순서를 위해 여행 타임존을 지정하세요" 경고를 1회 표시한다(분석을 차단하지 않는다).
> - **알려진 한계(v1 범위 밖):** 오프셋 태그가 없는 **다중 타임존 여행**은 단일 타임존 기준이라
>   타임존 경계/DST 부근에서 순서가 어긋날 수 있다(허용 한계).
> - 동일 초단위 타임스탬프(연사)는 파일명/식별자 순으로 안정적 tiebreak.

### 4.3 AI 랜드마크/위치 인식 엔진
- **메인: Gemini API (Vision)** — 사진 → `{ landmarkName, city, country, confidence }`.
- **자동 폴백: 현행 OpenAI 멀티모달 Vision 모델** — Gemini가 인식 실패하거나 신뢰도가 임계값 미만이면 자동 재시도.
  - 모델 ID는 **빌드 시점의 사용 가능 모델 목록에서 확정**하고, 죽은 ID로 하드코딩하지 않는다.
    (구 GPT-4o는 2026-02-16 API 종료됨.) **빌드 시점에 적합 vision 모델을 못 찾으면 빌드 실패**로 처리한다.
- **출력은 네이티브 스키마 강제 사용** — Gemini `responseSchema` / OpenAI structured outputs로
  JSON 스키마를 강제한다(프롬프트 텍스트로만 JSON을 유도하지 않는다).
- **좌표는 AI가 직접 만들지 않는다.** AI가 반환한 *이름/도시*를 지오코딩하여 플롯한다(4.4 분기 참조).
  - **disambiguation:** 지오코딩 결과가 다수(동명 지명)거나 신뢰도 임계값 미만이면 해당 사진을
    "이름만(위치 미상)"으로 분류해(4.6) 확신에 찬 오답 핀을 막는다.
- 두 엔진은 **공통 인터페이스(VisionProvider)** 뒤에 두어, 동일 스키마로 반환하도록 추상화.
- **엔진 모드 옵션:** 기본은 듀얼(Gemini → OpenAI 폴백). 설정에서 **단일 모델 모드**로 전환 가능.

```
[사진] → VisionProvider
            ├─ GeminiProvider (1차, responseSchema)
            └─ OpenAiProvider (폴백, structured outputs; 단일 모드 시 비활성)
        → { landmarkName, city, country, confidence }
        → 지오코딩(아래 4.4 분기) → { lat, lng }
```

### 4.4 지도 표시 및 좌표화
**좌표화(geocoding) 분기 — 입력 유형별로 다른 API 사용:**
- **랜드마크/POI 이름**(예: "에펠탑") → **Places API (Text Search)**. 모호·의미 기반 질의에 강함.
- **도시/국가 등 행정 지명 문자열** → **Geocoding API**. (Geocoding은 모호 질의에 약해 zero result 가능하므로
  POI 이름에는 쓰지 않는다.)

**지도 렌더링:**
- **3D 비행(best-effort): Maps 3D SDK for Android (Experimental Preview)** — `flyTo`/`flyAround` 카메라 경로.
- **기본/폴백: 표준 Google Maps Android SDK** — 2D/위성/마커·폴리라인. 제품 핵심 가치는 이 경로만으로 완결.
- 위치별 핀(마커) + 촬영 시각순 경로 폴리라인.
  - **GPS 핀과 AI 근사 위치 핀을 시각 구분**(예: AI 핀은 점선/반투명 + "근사 위치" 라벨).
- 사용자가 일반 2D / 위성 뷰로 전환 가능.

**3D 강등 계약 (Degradation Contract) — #3 핵심:**
- 다음 중 하나라도 발생하면 **자동으로 표준 Maps SDK 2D 폴리라인 리플레이로 전환**하고 사용자에게 1회 고지한다:
  - 3D SDK 초기화 실패 / 기기·API 미지원
  - Experimental Preview 종료 또는 GA 과금 전환 감지
- 3D는 "best-effort 부가 기능"으로 격하한다. 3D 미가용은 **오류가 아니라 정상 강등**이다.
- **GA 과금 전환 대비:** 3D 타일 호출 비용도 앱 내 비용 집계(4.8) 대상에 포함하거나, GA 후 **3D 기본 OFF**로
  전환하는 정책을 사전 결정한다(결정 #5 비용 상한과의 충돌 방지).
- **(위험 고지)** sideload APK는 자동 업데이트 경로가 없어, Experimental API의 breaking change 시
  사용자가 수동 재설치하기 전까지 3D가 깨질 수 있음을 README/앱 내에 고지한다.

### 4.5 경로 리플레이 (슬라이드쇼)
- 사진을 **촬영 시각(UTC 정규화) 순**으로 정렬해 경로 구성(4.2 참조).
- **자동 재생:** 카메라가 다음 위치로 부드럽게 이동(3D 가용 시 카메라 비행 + tilt, 아니면 2D 팬/줌),
  도착 시 해당 사진을 카드/팝업으로 표시 후 일정 시간 뒤 다음으로 이동.
- **수동 제어:** 재생/일시정지 + 이전/다음 버튼 (영상 플레이어식 UI).

### 4.6 위치 미상 / 부분 정보 사진 처리
- 다음 경우를 **각각 구분**해 처리한다:
  - **위치 미상** (GPS·AI 모두 실패): 지도/경로에서 제외, "위치 미상" 섹션에 모음.
  - **이름만(지오코딩 실패 또는 disambiguation 실패)**: "위치 미상"과 별도 표시(이름 노출), 향후 수동 핀 대상.
  - **위치는 있으나 촬영 시각 없음**: 경로 순서에서 제외하되 핀은 표시(별도 그룹/순서 끝).
- (향후) 지도를 길게 눌러 수동으로 위치를 지정하는 기능 추가 여지.

### 4.7 여행 저장 및 캐싱
- 분석 결과(사진 식별자, 좌표, 랜드마크명, 촬영 시각, 위치 출처[GPS/AI])를 **로컬 DB(Room/SQLite)**에 저장.
- **캐시·저장 키 = `MediaStore _ID` + 콘텐츠 해시 조합**(휘발성 URI는 영구 키로 사용 금지).
  - **해시 시점:** 선택 직후 일괄 해싱하지 않고, 분석 파이프라인에서 사진을 스트림으로 읽는 김에 계산해
    중복 I/O를 피한다(성능 예산 §5 반영).
  - 캐시는 **기기 로컬 한정**(`_ID`는 기기 내에서만 안정적). 기기 변경/초기화 후에는 재분석될 수 있음.
- "2024 파리 여행" 같은 이름으로 **여행 단위 저장** → 언제든 재생.
- 같은 사진은 결과를 **캐싱**하여 재분석/재호출 방지 (속도·API 비용 절감).

### 4.8 진행 상태 및 비용 통제
- 분석 중 진행률 표시 (예: "23 / 100장 분석 중").
- **클라이언트측 처리 정책 (구체 수치):**
  - 동시 Vision/지오코딩 콜 **최대 4건**(레이트리밋 여유 확보).
  - 콜당 **타임아웃 30초**.
  - 실패 시 **재시도 최대 2회**, 지수 백오프(1s → 2s → 4s) + 지터.
  - 429/할당량 응답 시 백오프 연장 + 사용자 안내.
- **여행당 API 콜/비용 상한**을 앱 내에서 설정·집계(콘솔 quota cap과 별개). 상한 도달 시 분석 중단·안내.

---

## 5. 비기능 요구사항 (Non-Functional)

| 항목 | 요구사항 |
|------|---------|
| 성능 | ~100장 처리. GPS 있는 사진은 AI 호출 없이 즉시 처리. 비전·지오코딩 콜은 동시 4건·타임아웃 30s·재시도 2회. 콘텐츠 해시는 분석 스트림 중 1회 계산. |
| 비용 통제 | 콘솔 **quota cap** + 앱 내 **여행당 콜/비용 상한** 이중 적용. 3D GA 전환 시 타일 비용도 집계 대상에 포함. |
| 보안 | 개인/가족용이므로 API 키 앱 내장 허용. **단, 앱측 여행당 상한은 도난 키를 막지 못한다**(공격자는 앱을 우회해 API 직접 호출). **도난 키의 비용 피해를 제한하는 것은 콘솔측 quota cap뿐이다** — 콘솔 quota를 필수 안전장치로 둔다. 이 잔여 위험은 가족용 범위에서 수용. |
| 오프라인 | 저장된 여행은 AI 호출 없이 **저장 메타데이터(좌표·시각·랜드마크명)로 재생** 가능. 단, **지도 타일(2D·3D 모두)은 네트워크 필요** — 진정한 오프라인 지도가 아니라 연결이 없으면 빈/실패한 지도 위 폴리라인만 그려진다. |
| 프라이버시 | 사진은 인식 시에만 외부 API(Google·OpenAI)로 전송. **전송 파이프라인 순서 고정: ① 원본에서 GPS/시각 EXIF 로컬 읽기 → ② 다운스케일(재인코딩) → ③ 업로드.** **모든 원본은 포맷 무관 업로드 전 항상 JPEG로 재인코딩**하며, 이 재인코딩이 EXIF 제거를 보장한다(ExifInterface의 스트립은 JPEG 전용이고 HEIC/RAW엔 동작하지 않으므로 별도 스트립 단계에 의존하지 않는다). 결과는 기기 로컬에만 저장. |

---

## 6. 기술 스택 (제안)

| 영역 | 선택 |
|------|------|
| 플랫폼 | Android (Java, View/XML 기반 — Fragment + Navigation Component + ViewBinding) |
| 사진 접근 | MediaStore + `READ_MEDIA_IMAGES` + `ACCESS_MEDIA_LOCATION` (`setRequireOriginal()`) |
| EXIF 파싱 | `androidx.exifinterface` |
| AI 인식 | Gemini API (메인, responseSchema) + 현행 OpenAI Vision (폴백, structured outputs) — 이름/도시만 반환 |
| 좌표화 | Places API (Text Search, POI 이름) + Geocoding API (행정 지명) |
| 지도 | Maps 3D SDK for Android (Experimental, best-effort 3D) + 표준 Maps Android SDK (2D/위성 기본·폴백) |
| 로컬 저장 | Room (SQLite, Java annotationProcessor) |
| 비동기/직렬화 | ExecutorService(동시성) + Retrofit/OkHttp + Gson |
| DI | Hilt (Java annotationProcessor) |

---

## 7. MVP 범위와 향후 과제

### MVP 포함
- 갤러리 다중 선택(MediaStore), EXIF GPS/시각 추출(원본 GPS 권한 포함)
- Gemini 인식 + OpenAI 폴백(이름/도시, 스키마 강제) → Places/Geocoding 좌표화, 단일 모델 모드 토글
- 지도 핀 + 촬영 시각순 경로(GPS/AI 핀 구분), 2D 리플레이(필수) + 3D 비행(best-effort)
- 자동/수동 슬라이드쇼
- 여행 저장 + 캐싱, 진행률 표시, "위치 미상/이름만/시각 없음" 구분
- 여행당 API 비용 상한, 동시성·재시도·타임아웃

### 향후 과제 (Post-MVP)
- 위치 미상/이름만 사진 **수동 핀 지정**
- 갤러리 전체 자동 스캔(수백~수천 장)
- 백엔드 서버 경유(상용 배포 전환 시 키 보안 강화)
- iOS 확장, 여행 공유 기능
- 다중 타임존 여행 정밀 순서 처리

---

## 8. 알려진 제약 (Constraints / Risks)

- **GPS 없는 사진의 위치는 추정치** — AI는 *이름/도시*만 추론하고 좌표는 지오코딩으로 얻으므로
  도시 단위 정확도일 수 있음. 근사 경로 출시는 수용된 결정.
- **AI 인식 실패 가능** — 실내·음식·추상 풍경 사진은 "위치 미상" 처리됨.
- **API 키 내장 위험** — 디컴파일로 키 추출 가능. **앱측 상한은 도난 키를 못 막고, 콘솔 quota cap만 비용 피해를 제한**.
- **Maps 3D SDK는 Experimental** — breaking change/철회/GA 과금 전환 위험. 2D 폴백으로 강등(4.4 계약). 핵심 가치는 2D로 완결.
- **모델 수명 리스크** — 비전 모델은 폐기될 수 있으므로 모델 ID를 하드코딩하지 않고 빌드 시점 확정.
- **EXIF 타임존 부재** — 오프셋 없는 다중 타임존 여행은 순서 오류 가능(v1 허용 한계).
- **광범위 미디어 권한** — `READ_MEDIA_IMAGES`는 Play 스토어 정식 배포를 제약(sideload 범위에서 수용).

---

## 9. 결정 기록 (Decision Log)

| # | 결정 항목 | 선택 |
|---|----------|------|
| 1 | 플랫폼 | Android 전용 |
| 2 | 핵심 사용자 | 여행을 즐기는 일반인 |
| 3 | 사진 소스 | 기기 갤러리 (MediaStore, 영구 재접근/캐싱 목적, 원본 GPS 권한 포함) |
| 4 | 인식 엔진 | Gemini 메인 + 현행 OpenAI Vision 폴백 (이름/도시만, 스키마 강제, 좌표는 지오코딩) |
| 5 | API 키 관리 | 앱 내장 + 콘솔 quota cap(도난 키 피해 범위 제한, 방어 아님) + 앱 내 여행당 비용 상한 |
| 6 | 배포 범위 | 개인/가족용 (APK 직접 설치; 광범위 권한이라 Play 정식 배포 비대상) |
| 7 | 위치 우선순위 | EXIF GPS 우선 → 없으면 AI 이름 추론 → Places/Geocoding 분기 좌표화 |
| 8 | 경로 순서 | 촬영 시각 UTC 정규화순 (오프셋 없으면 여행 기준 타임존; 다중 타임존은 v1 한계) |
| 9 | 슬라이드쇼 | 자동 재생 + 수동 제어 |
| 10 | 지도 스타일 | 2D/위성 기본·필수 + Maps 3D SDK 비행(Experimental, best-effort, 강등 계약) |
| 11 | 데이터 저장 | 여행 단위 저장 + 캐싱 (키 = MediaStore _ID + 콘텐츠 해시, 기기 로컬) |
| 12 | 처리 규모 | ~100장, 진행률, 동시 4건·타임아웃 30s·재시도 2회·비용 상한 |
| 13 | 위치 미상 사진 | "위치 미상 / 이름만 / 시각 없음" 구분 표시 |
| 14 | 엔진 모드 | 듀얼 폴백 기본 + 단일 모델 모드 전환 옵션 |
| 15 | 프라이버시 전송 | 원본 EXIF 로컬 읽기 → 다운스케일(JPEG 재인코딩, EXIF 제거 보장) → 업로드 (순서 고정) |
