# TravelTrace ProGuard rules.
# minify is disabled for the sideload/family build (see app/build.gradle.kts).
# Add keep rules here when enabling R8 (e.g. for kotlinx.serialization @Serializable models).
