<?xml version="1.0" encoding="utf-8"?>
<!-- 사진첩 선택 드로어: 핸들 + 제목 + 스크롤 가능한 앨범 목록.
     행은 기기마다 개수가 다르니 코드로 동적 생성한다(UnknownPhotosSheetFragment 의
     톤 타일 생성과 같은 패턴) — 앨범 수가 많아도 NestedScrollView 로 잘려 보이지 않는다. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/surface"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingTop="22dp"
    android:paddingEnd="20dp"
    android:paddingBottom="12dp">

    <View
        android:layout_width="@dimen/sheet_handle_width"
        android:layout_height="@dimen/sheet_handle_height"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="@drawable/handle_bottom_sheet" />

    <TextView
        android:id="@+id/albumSheetTitle"
        style="@style/TextAppearance.TravelTrace.Title2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/select_album_sheet_title" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/albumRows"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </androidx.core.widget.NestedScrollView>
</LinearLayout>
